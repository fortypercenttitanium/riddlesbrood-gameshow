import styled from 'styled-components';

const GameboardContainer = styled.div`
	height: 100vh;
	width: 100vw;
	margin: 0;
	padding: 0;
	overflow: hidden;
	pointer-events: none;
`;

export { GameboardContainer };
